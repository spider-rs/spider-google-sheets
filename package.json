{
  "name": "spider-google-sheets-addon",
  "version": "1.0.0",
  "description": "Spider for Google Sheets — custom functions and menu actions for web scraping",
  "scripts": {
    "push": "npx clasp push",
    "open": "npx clasp open",
    "watch": "npx clasp push --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.4.0"
  }
}
